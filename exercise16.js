class Person {
    constructor(firstName, lastName) {
      this.firstName = firstName;
      this.lastName = lastName;
    }
  }
  const developer = new Person('Arshya', 'Razavi');
  console.log(developer.firstName + ' ' + developer.lastName);
  